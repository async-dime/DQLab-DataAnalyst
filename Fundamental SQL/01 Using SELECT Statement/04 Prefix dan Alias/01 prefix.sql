-- Gantilah seluruh code yang ada pada code editor dengan perintah SELECT 
-- untuk menampilkan kolom kode_produk dari tabel ms_produk dengan penulisan menggunakan prefix nama tabel.

SELECT ms_produk.kode_produk FROM ms_produk;